{
  "name": "zk-blockchain-integration-test",
  "private": true,
  "type": "module",
  "description": "Headless end-to-end test for packages/blockchain (PLAN.md Stage 6). Plays the role of a browser to generate one real ZK proof and drive a full election lifecycle over the REST API — never part of the running node or any production code path.",
  "scripts": {
    "test": "node run.mjs"
  },
  "dependencies": {
    "@aztec/bb.js": "0.82.0",
    "@noir-lang/noir_js": "1.0.0-beta.3",
    "@zk-kit/lean-imt": "^2.2.4",
    "poseidon-lite": "^0.3.0"
  }
}
